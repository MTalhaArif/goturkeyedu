import PageClient from "./page-client";

export const metadata = {
  title: "Türkiye Scholarships - Coverage & Eligibility | Go Turkey",
  description: "Everything about the Türkiye Scholarships program: tuition, stipend, flights, health insurance and accommodation coverage, and application criteria.",
  alternates: {
    canonical: "/for-students/scholarships",
  },
};

export default function Page() {
  return <PageClient />;
}
